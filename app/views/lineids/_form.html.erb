<%= form_with(model: lineid) do |form| %>
  <% if lineid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lineid.errors.count, "error") %> prohibited this lineid from being saved:</h2>

      <ul>
        <% lineid.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :line_id %>
    <%= form.text_field :line_id %>
  </div>

  <div class="field">
    <%= form.label :flag %>
    <%= form.text_field :flag %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
